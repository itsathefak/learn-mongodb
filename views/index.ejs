<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Whatsapp</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h3>All Chats!</h3>

    <form method="get" action="/chats/new">
        <button>New Chat</button>
    </form>
    <br><br>
    <% for ( let chat of chats ){ %>
        <div class="chat">
            <p>From : <i>
                    <%= chat.from %>
                </i>
            </p>
            <div class="message">
                <p>
                    <%= chat.message %>
                </p>
            </div>
            <p>Recived by : <i>
                    <%= chat.to %>
                </i>
            </p>
            <hr>
            <p>
                <%= chat.createdAt.toString().split(" ")[4] %>
            </p>
            <p><%= chat.createdAt.toString().split(" ").slice(0,4).join(" ") %>
            </p>
            <br><br>
        </div>

        <% } %>

</body>

</html>